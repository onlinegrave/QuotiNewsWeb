import React from 'react';
import { CountryCode } from '../services/types';
import { useSelector } from 'react-redux';
import { RootState } from '../store';
// const COUNTRY_LIST: { [key: CountryCode]: string } = {
//   "en": "What"

// }

const Language = () => {
  // const country = useSelector((state: RootState) => state.app.country.value)
  return <div>
    <div>Language</div>
  </div>;
}

export default Language;
